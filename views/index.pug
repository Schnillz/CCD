extends layout

block content

  div.container
    div.page-header
      h1= counter
      h1= message
    form(name="start_game", method="post")
      div.form-group
        label(for="number_of_matches") Anzahl der Streichhölzer eintragen
        input(class="form-control", type="text", name="number_matches")
      div.form-group
        label Welcher Spieler soll beginnen?
        div.radio
          label
            input(type="radio", name="which_player", value="Spieler A")
            span Spieler A
        div.radio
          label
            input(type="radio", name="which_player", value="Spieler B")
            span Spieler B
      button(class="btn btn default", type="submit") Spiel beginnen
